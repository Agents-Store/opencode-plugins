# Local test artifacts from skill-creator eval runs — not part of plugin distribution.
# These contain instance-specific tool prefixes (e.g. resolved via ToolSearch during testing)
# and must never be shipped with the plugin, which must stay tool-agnostic.
skills/*-workspace/
.skill-workspaces/

# Local settings
.claude/*.local.md
.env
.env.local
