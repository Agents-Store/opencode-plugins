{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/macstacks/macstack/main/schema/macstack.schema.json",
  "title": "macstack.json",
  "$comment": "rev 15 - 2026-08-27: `source` added to workflows[] - the path, relative to the project root, where this workflow lives in code. Names do not bridge the two sides: measured on a live project, code names a workflow for its domain (`attendance`) and the spec names it for its step (`wf-entry-capture`), and only 3 of 17 match. Both namings are right and no normalisation reconciles them, so without the field a code audit either guesses or reports 14 of 17 as missing from a codebase that has all of them - a confident number that is wrong, and work gets scheduled against it. One path may carry more than one workflow; `source` says where it lives, not that it lives there alone. Additive: format version stays 1.0, no $def changed. Mirrors: plugins/macstack-dev/skills/lint/references/ and VK-OPS/docs/macstack/ - canon macstacks/macstack is pushed separately by the owner. ",
  "description": "MACSTACK stack file: meta-information for configuring a Claude project + the business and technical specification of the stack in one standardized JSON. Lives in the project root; CLAUDE.md references it; the macstack-dev Claude plugin (agents.store) parses the file and scaffolds the project structure (nextjs in software → Next.js skeleton). Inheritance: prototype points to a parent macstack.json (GitHub or absolute path); the current file extends/overrides the parent — merge by id (see the spec). Section order is result-first: goals → results → processes → triggers → workflows → software → entities → interfaces → connections → agents → context → resources.",
  "type": "object",
  "required": [
    "macstack",
    "name",
    "version",
    "description"
  ],
  "properties": {
    "macstack": {
      "type": "string",
      "description": "File format version",
      "enum": [
        "1.0"
      ]
    },
    "name": {
      "$ref": "#/$defs/slug",
      "description": "Project/stack slug"
    },
    "version": {
      "type": "string",
      "description": "Semver of this specification"
    },
    "description": {
      "type": "string"
    },
    "prototype": {
      "$ref": "#/$defs/stackRef",
      "description": "Reference to the parent macstack.json (the stack prototype) that this file inherits (extend/override, merge by id). GitHub OR a local folder (absolute path). Chains are allowed, cycles are an error"
    },
    "prototype_exclude": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z_]+\\.[a-z0-9]+([-.][a-z0-9]+)*$"
      },
      "description": "Prototype elements that are NOT inherited: '<section>.<id>' (e.g. 'software.qdrant', 'workflows.wf-x')"
    },
    "stacks": {
      "type": "object",
      "description": "This stack's place among the OTHER live macstack.json stacks of the organization. NO section = standalone stack outside any organization. NOT to be confused with prototype (template inheritance): prototype = \"what it is made from\", stacks = \"what it is connected to\". Organization model: one ROOT stack (the organization's core workspace — usually composable/Agents Workspace; from it a stack agent sees and orchestrates all stacks) + SUBSTACKS (sub-stacks: web apps, CRM, bots — usually application/agents by profile.type; service stacks: shared workflow backend). Cross-stack references in any ref field: '<stack-id>:<element-id>' (e.g. master: 'meg-root:postgresql', uses: 'workflows-hub:wf-enrich'), where stack-id is declared here",
      "properties": {
        "role": {
          "enum": [
            "root",
            "substack"
          ],
          "description": "root = the organization's root stack (single; holds the substacks registry); substack = an organization sub-stack registered in root (web app, CRM, bot, service stack)"
        },
        "root": {
          "type": "object",
          "description": "For role=substack: reference to the organization's root stack",
          "required": [
            "id",
            "ref"
          ],
          "properties": {
            "id": {
              "$ref": "#/$defs/slug",
              "description": "Local identifier of the root stack (namespace for cross-stack references)"
            },
            "ref": {
              "$ref": "#/$defs/stackRef"
            }
          }
        },
        "substacks": {
          "type": "array",
          "description": "For role=root: registry of the organization's sub-stacks",
          "items": {
            "type": "object",
            "required": [
              "id",
              "ref"
            ],
            "properties": {
              "id": {
                "$ref": "#/$defs/slug"
              },
              "ref": {
                "$ref": "#/$defs/stackRef"
              },
              "note": {
                "type": "string",
                "description": "E.g.: company website, CRM, content stack"
              }
            }
          }
        },
        "links": {
          "type": "array",
          "description": "Subject-matter links to specific stacks (in addition to root/substacks)",
          "items": {
            "type": "object",
            "required": [
              "id",
              "ref",
              "relation"
            ],
            "properties": {
              "id": {
                "$ref": "#/$defs/slug",
                "description": "Local identifier (namespace for '<stack-id>:<element-id>')"
              },
              "ref": {
                "$ref": "#/$defs/stackRef"
              },
              "relation": {
                "enum": [
                  "uses",
                  "feeds",
                  "peer"
                ],
                "description": "uses = we consume its workflows/data/MCP; feeds = we push data/results to it; peer = a neighboring stack with no dependency"
              },
              "what": {
                "type": "string",
                "description": "What exactly connects them (shared workflow backend, shared CRM database…)"
              }
            }
          }
        }
      }
    },
    "identity": {
      "type": "object",
      "description": "Project identity",
      "properties": {
        "title": {
          "type": "string"
        },
        "client": {
          "type": "string"
        },
        "organization": {
          "$ref": "#/$defs/slug",
          "description": "Organization that owns the stack; the Manager groups stacks by it and builds the root/substacks map"
        },
        "kind": {
          "enum": [
            "template",
            "project",
            "workspace",
            "demo"
          ]
        },
        "business_domain": {
          "type": "string",
          "description": "Functional domain — which business function the stack automates (canonical 12: sales, marketing, hr, devops, finance, support, ecommerce, education, product, services, ops, engineering). This is the FUNCTION axis. The client's market vertical is a separate axis — see `industry`."
        },
        "industry": {
          "type": "string",
          "description": "Market vertical of the client the stack serves, kebab-case slug (seeded from the agents.store catalogue: real-estate, legal, healthcare, logistics). Extensible: a new vertical is a new slug. This is the VERTICAL axis — do not put a business function here, that belongs in `business_domain`. Omit for internal workspaces with no external client. A `sales` stack built for a real-estate agency is business_domain=sales + industry=real-estate."
        },
        "business_archetype": {
          "type": "string"
        },
        "domains": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "sites": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "slug",
              "domain"
            ],
            "properties": {
              "slug": {
                "$ref": "#/$defs/slug"
              },
              "domain": {
                "type": "string"
              },
              "devPort": {
                "type": "number"
              },
              "status": {
                "enum": [
                  "active",
                  "planned",
                  "paused"
                ]
              }
            }
          }
        }
      }
    },
    "profile": {
      "type": "object",
      "description": "Stack classification per Stack Architecture. stack_level L0–L3 is a complexity property of the stack (not a template hierarchy)",
      "properties": {
        "type": {
          "enum": [
            "composable",
            "application",
            "agents"
          ]
        },
        "category": {
          "enum": [
            "web_application",
            "crm",
            "content",
            "data_automation"
          ],
          "description": "Only for type=application"
        },
        "stack_level": {
          "enum": [
            "L0",
            "L1",
            "L2",
            "L3"
          ]
        },
        "stack_version": {
          "type": "string",
          "pattern": "^v[0-9]+$",
          "description": "Only for type=composable (v1/v2/v3)"
        },
        "architecture_patterns": {
          "type": "array",
          "items": {
            "enum": [
              "database-first",
              "hub-and-spoke",
              "event-bus",
              "api-gateway",
              "rag",
              "multi-agent"
            ]
          }
        },
        "development_approach": {
          "enum": [
            "low-code",
            "no-code",
            "custom",
            "hybrid"
          ]
        },
        "purposes": {
          "type": "array",
          "items": {
            "enum": [
              "workflow_execution",
              "code_development",
              "artifact_generation",
              "domain_knowledge"
            ]
          }
        },
        "operation_modes": {
          "type": "array",
          "items": {
            "enum": [
              "ui",
              "agent_os",
              "api_mcp",
              "hybrid"
            ]
          }
        },
        "upgrade_path": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "add": {
                "type": "array",
                "items": {
                  "$ref": "#/$defs/slug"
                }
              },
              "gives": {
                "type": "string"
              }
            }
          }
        }
      }
    },
    "goals": {
      "type": "array",
      "description": "Top-level business goals (the business vector). Goal = a direction/ambition with a horizon; Results are the measurable assets that realize it. Link: results[].goal → goals[].id; a goal with no results is a linter warning (\"a goal with no path to it\")",
      "items": {
        "type": "object",
        "required": [
          "id",
          "name"
        ],
        "properties": {
          "id": {
            "$ref": "#/$defs/slug"
          },
          "name": {
            "type": "string"
          },
          "statement": {
            "type": "string",
            "description": "Goal statement (e.g.: reach $20k MRR by Q2)"
          },
          "horizon": {
            "type": "string",
            "description": "Horizon/deadline (YYYY-MM-DD or Q2-2027)"
          },
          "metric": {
            "type": "object",
            "properties": {
              "unit": {
                "type": "string"
              },
              "target": {
                "type": [
                  "number",
                  "string"
                ]
              }
            }
          },
          "owner": {
            "type": "string"
          },
          "status": {
            "$ref": "#/$defs/status"
          }
        }
      }
    },
    "results": {
      "type": "array",
      "description": "LENS 1: what results the stack produces. Result = an asset that exists and makes/saves money. Named, measured, dated. Each result may reference the goal it realizes",
      "items": {
        "type": "object",
        "required": [
          "id",
          "name",
          "class"
        ],
        "properties": {
          "id": {
            "$ref": "#/$defs/slug"
          },
          "name": {
            "type": "string"
          },
          "class": {
            "enum": [
              "revenue_asset",
              "client_revenue",
              "pipeline_asset",
              "cost_saving"
            ]
          },
          "metric": {
            "type": "object",
            "properties": {
              "unit": {
                "type": "string",
                "description": "$/mo, leads/mo, hours saved/mo, posts/week…"
              },
              "target": {
                "type": [
                  "number",
                  "string"
                ]
              },
              "cadence": {
                "enum": [
                  "recurring",
                  "one_time"
                ]
              }
            }
          },
          "value_usd_month": {
            "type": "string",
            "description": "Range/estimate; for pipeline — mark as indirect"
          },
          "business_domain": {
            "type": "string"
          },
          "problem": {
            "type": "string",
            "description": "The client's original problem this result closes"
          },
          "description": {
            "type": "string"
          },
          "example": {
            "type": "string"
          },
          "goal": {
            "type": "string",
            "description": "→ goals[].id — which business goal the result realizes"
          },
          "produced_by": {
            "$ref": "#/$defs/refList",
            "description": "→ processes[].id"
          },
          "feeds": {
            "$ref": "#/$defs/refList",
            "description": "Result becomes a Resource: → results[].id | processes[].id"
          },
          "offer": {
            "type": "string",
            "description": "Reference to the offer (Offer Catalog)"
          },
          "status": {
            "$ref": "#/$defs/status"
          },
          "review_date": {
            "type": "string"
          }
        }
      }
    },
    "processes": {
      "type": "array",
      "description": "LENS 2: business processes — the main unit of value. Described by skills; consist of tasks, each backed by a workflow or a human",
      "items": {
        "type": "object",
        "required": [
          "id",
          "name",
          "type"
        ],
        "properties": {
          "id": {
            "$ref": "#/$defs/slug"
          },
          "name": {
            "type": "string"
          },
          "type": {
            "enum": [
              "development",
              "operations",
              "provisioning"
            ]
          },
          "automation_mode": {
            "enum": [
              "workflow",
              "agent",
              "hybrid"
            ]
          },
          "skill": {
            "type": "string",
            "description": "SKILL identifier (plugin:skill) implementing the process"
          },
          "description": {
            "type": "string"
          },
          "lifecycle": {
            "type": "string",
            "description": "Textual state machine of the process"
          },
          "schedule": {
            "type": "string",
            "description": "Process cadence (cron / weekly / on-event)"
          },
          "tasks": {
            "type": "array",
            "items": {
              "type": "object",
              "required": [
                "id"
              ],
              "properties": {
                "id": {
                  "$ref": "#/$defs/slug"
                },
                "name": {
                  "type": "string"
                },
                "workflow": {
                  "type": "string",
                  "description": "→ workflows[].id"
                },
                "human": {
                  "type": "object",
                  "description": "Human-in-the-loop point",
                  "properties": {
                    "role": {
                      "type": "string",
                      "description": "→ roles[].id"
                    },
                    "gate": {
                      "enum": [
                        "approve",
                        "input",
                        "review",
                        "execute"
                      ]
                    }
                  }
                }
              }
            }
          },
          "produces": {
            "$ref": "#/$defs/refList",
            "description": "→ results[].id"
          },
          "status": {
            "$ref": "#/$defs/status"
          }
        }
      }
    },
    "triggers": {
      "type": "array",
      "description": "A separate collection of the stack's triggers. Triggers live in DIFFERENT software (cron in trigger-dev, webhook in n8n, db-event in NocoBase/Directus). Workflows and agents reference triggers by id; all trigger settings live here, in config",
      "items": {
        "type": "object",
        "required": [
          "id",
          "type"
        ],
        "properties": {
          "id": {
            "$ref": "#/$defs/slug",
            "description": "Trigger code (trg-nightly-0300, trg-lead-created)"
          },
          "name": {
            "type": "string"
          },
          "type": {
            "enum": [
              "schedule",
              "webhook",
              "db_event",
              "form",
              "email",
              "queue",
              "manual"
            ],
            "description": "schedule = cron; webhook = incoming HTTP; db_event = data change; form = form submission; email/queue = incoming messages; manual = started by a human/agent"
          },
          "software": {
            "type": "string",
            "description": "→ software[].id — where the trigger lives/listens"
          },
          "instance": {
            "type": "string",
            "description": "→ software[].instances[].id"
          },
          "config": {
            "type": "object",
            "description": "Trigger settings: {schedule: '0 3 * * *'} | {path: '/hooks/x', method} | {entity, event: create|update|delete, condition} | {form: id} | {queue: name}…",
            "additionalProperties": true
          },
          "status": {
            "$ref": "#/$defs/status"
          }
        }
      }
    },
    "workflows": {
      "type": "array",
      "description": "LENSES 2-3: deterministic software implementations, invoked via MCP/API/CLI or by triggers. Naming: [Domain] - [Action] - [Trigger]",
      "items": {
        "type": "object",
        "required": [
          "id",
          "name",
          "engine"
        ],
        "properties": {
          "id": {
            "$ref": "#/$defs/slug"
          },
          "name": {
            "type": "string"
          },
          "engine": {
            "$ref": "#/$defs/slug",
            "description": "n8n | trigger-dev | nocobase-workflows | directus-flows | script | custom…"
          },
          "software": {
            "type": "string",
            "description": "→ software[].id where the workflow lives"
          },
          "triggers": {
            "$ref": "#/$defs/refList",
            "description": "→ triggers[].id — codes of the triggers that start this workflow (settings live in the triggers collection)"
          },
          "execution": {
            "enum": [
              "instant",
              "background"
            ],
            "description": "Execution character: instant response or durable background"
          },
          "invocation": {
            "type": "array",
            "items": {
              "enum": [
                "mcp",
                "api",
                "cli",
                "webhook",
                "trigger",
                "internal"
              ]
            },
            "description": "Invocation channels: by an agent (mcp/api/cli), externally (webhook), by a trigger (trigger), inside the platform (internal)"
          },
          "implements": {
            "type": "string",
            "description": "→ processes[].id or a task id"
          },
          "uses": {
            "$ref": "#/$defs/refList",
            "description": "→ software[].id | entities[].id"
          },
          "location": {
            "type": "string",
            "description": "Where the code/config lives: repo path, instance, external backend"
          },
          "retry": {
            "type": "string"
          },
          "status": {
            "$ref": "#/$defs/status"
          },
          "technical": {
            "type": "boolean",
            "default": false,
            "description": "True when this record is infrastructure: it exists in the system but has no place in the client documents. Everything without the flag is expected to be named there, and its absence is a finding."
          },
          "source": {
            "type": "string",
            "description": "Path from the project root to where this workflow lives in code, e.g. `src/workflows/attendance.ts`. Names alone do not connect the two sides: code names a workflow for its domain, the spec for its step. One file may hold more than one workflow."
          }
        }
      }
    },
    "software": {
      "type": "array",
      "description": "LENSES 3, 5: all software of the stack (self-hosted, SaaS, and the client's external systems). category and type are mandatory — see the software taxonomy in the spec",
      "items": {
        "type": "object",
        "required": [
          "id",
          "category",
          "type",
          "layers"
        ],
        "properties": {
          "id": {
            "$ref": "#/$defs/slug",
            "description": "Canonical slug (postgresql, nocobase, trigger-dev)"
          },
          "name": {
            "type": "string"
          },
          "category": {
            "$ref": "#/$defs/slug",
            "description": "Category from the canonical registry registry/software-categories.json (20 base + extensions: accounting, custom-scripts…). The linter checks against the registry"
          },
          "type": {
            "enum": [
              "ready_made",
              "constructor",
              "framework",
              "library",
              "custom"
            ],
            "description": "Nature of the software: ready-made application / low-code constructor / framework / library / our custom software"
          },
          "form": {
            "type": "array",
            "items": {
              "enum": [
                "web",
                "cli",
                "desktop",
                "mobile",
                "api_service",
                "library"
              ]
            }
          },
          "license": {
            "enum": [
              "open_source",
              "saas",
              "proprietary"
            ]
          },
          "layers": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": {
              "enum": [
                "data",
                "logic",
                "interface",
                "infrastructure"
              ]
            },
            "description": "Strictly 4 typed layers: data | logic | interface | infrastructure. Software may occupy several layers (nextjs = logic+interface)"
          },
          "role": {
            "type": "string",
            "description": "Role in this stack: Headless CMS, BPMS platform…"
          },
          "value": {
            "enum": [
              "interface",
              "workflows",
              "data",
              "storage"
            ],
            "description": "Why the software is in the stack (one of the 4 values)"
          },
          "hosting": {
            "enum": [
              "self_hosted",
              "saas_cloud",
              "external"
            ]
          },
          "instances": {
            "type": "array",
            "description": "Instances of this software — one software may have SEVERAL deployments (prod/dev, per client, per group company), each with its own URLs. Referenced by entities.stores[].instance, connections.mcp[].instance, interfaces[].instances",
            "items": {
              "type": "object",
              "required": [
                "id"
              ],
              "properties": {
                "id": {
                  "$ref": "#/$defs/slug",
                  "description": "Instance identifier (nocobase-prod, nocobase-dev, pg-main)"
                },
                "name": {
                  "type": "string"
                },
                "environment": {
                  "enum": [
                    "prod",
                    "dev",
                    "staging",
                    "demo"
                  ]
                },
                "url": {
                  "type": "string",
                  "description": "Public URL, if not secret"
                },
                "url_env": {
                  "type": "string",
                  "description": "NAME of the env key holding the URL (never the value!)"
                },
                "server": {
                  "type": "string",
                  "description": "Host/server"
                },
                "deploy": {
                  "type": "string",
                  "description": "dokploy | docker-compose | vercel | cloudron…"
                }
              }
            }
          },
          "agentic": {
            "type": "object",
            "description": "Agentic IT Ready passport: MCP + API + CLI. Each channel: true / false / \"partial\" (present with caveats — the ⚠️ of the Software Taxonomy). rating: full = 3×true, good = 2×true, basic = 1×true, partial = only partial channels, none = nothing",
            "properties": {
              "mcp": {
                "$ref": "#/$defs/agenticChannel"
              },
              "api": {
                "$ref": "#/$defs/agenticChannel"
              },
              "cli": {
                "$ref": "#/$defs/agenticChannel"
              },
              "rating": {
                "enum": [
                  "full",
                  "good",
                  "basic",
                  "partial",
                  "none"
                ]
              }
            }
          },
          "components": {
            "type": "array",
            "description": "Software Components inside the software",
            "items": {
              "type": "object",
              "required": [
                "type",
                "name"
              ],
              "properties": {
                "type": {
                  "enum": [
                    "table",
                    "collection",
                    "workflow",
                    "view",
                    "form",
                    "function",
                    "trigger",
                    "webhook",
                    "credential",
                    "dashboard",
                    "api",
                    "mcp_server",
                    "bot",
                    "page",
                    "campaign",
                    "bucket",
                    "other"
                  ]
                },
                "name": {
                  "type": "string"
                },
                "ref": {
                  "type": "string",
                  "description": "ID/slug inside the software itself (volatile IDs belong in project-config)"
                }
              }
            }
          },
          "plugins": {
            "type": "object",
            "description": "Plugin overrides (derived by default: {tool}-{dev|ops|provision})",
            "properties": {
              "dev": {
                "type": "string"
              },
              "ops": {
                "type": "string"
              },
              "provision": {
                "type": "string"
              }
            }
          },
          "cost": {
            "type": "object",
            "properties": {
              "model": {
                "type": "string",
                "description": "free | subscription | usage | one_time"
              },
              "usd_month": {
                "type": [
                  "number",
                  "string"
                ]
              }
            }
          },
          "technical": {
            "type": "boolean",
            "default": false,
            "description": "True when this record is infrastructure: it exists in the system but has no place in the client documents. Everything without the flag is expected to be named there, and its absence is a finding."
          }
        }
      }
    },
    "entities": {
      "type": "array",
      "description": "LENS 4: business entities — structure, Data Sources and the MASTER source of each",
      "items": {
        "type": "object",
        "required": [
          "id",
          "name",
          "master",
          "stores"
        ],
        "properties": {
          "id": {
            "$ref": "#/$defs/slug"
          },
          "name": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "attributes": {
            "type": "array",
            "items": {
              "type": "object",
              "required": [
                "name",
                "type"
              ],
              "properties": {
                "name": {
                  "type": "string"
                },
                "type": {
                  "type": "string",
                  "description": "string | number | boolean | date | enum | relation | file | json"
                },
                "required": {
                  "type": "boolean"
                },
                "values": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "description": "For enum"
                },
                "relation": {
                  "type": "string",
                  "description": "→ entities[].id for type=relation"
                }
              }
            }
          },
          "stores": {
            "type": "array",
            "minItems": 1,
            "description": "All Data Sources the entity lives in, and in what role",
            "items": {
              "type": "object",
              "required": [
                "software",
                "role"
              ],
              "properties": {
                "software": {
                  "type": "string",
                  "description": "→ software[].id"
                },
                "instance": {
                  "type": "string",
                  "description": "→ software[].instances[].id — the SPECIFIC instance the entity lives in (one entity may live in several instances: prod and dev, an instance per group company)"
                },
                "role": {
                  "enum": [
                    "master",
                    "replica",
                    "cache",
                    "interface",
                    "archive"
                  ]
                },
                "collection": {
                  "type": "string",
                  "description": "Table/collection name in this source"
                },
                "sync": {
                  "type": "string",
                  "description": "How it syncs with the master (workflow id / shared-db / manual)"
                }
              }
            }
          },
          "master": {
            "type": "string",
            "description": "→ software[].id — the single master source of the entity"
          },
          "state_fields": {
            "type": "array",
            "description": "Status fields that drive processes (statusVisa, statusOrd)",
            "items": {
              "type": "object",
              "required": [
                "field"
              ],
              "properties": {
                "field": {
                  "type": "string"
                },
                "states": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "drives": {
                  "type": "string",
                  "description": "→ processes[].id"
                }
              }
            }
          },
          "relations": {
            "$ref": "#/$defs/refList",
            "description": "→ entities[].id"
          },
          "volume": {
            "type": "string"
          },
          "technical": {
            "type": "boolean",
            "default": false,
            "description": "True when this record is infrastructure: it exists in the system but has no place in the client documents. Everything without the flag is expected to be named there, and its absence is a finding."
          }
        }
      }
    },
    "interfaces": {
      "type": "array",
      "description": "LENS 5: the stack's interfaces — human and agent-facing. Notifications = type: channel",
      "items": {
        "type": "object",
        "required": [
          "id",
          "type",
          "audience"
        ],
        "properties": {
          "id": {
            "$ref": "#/$defs/slug"
          },
          "name": {
            "type": "string"
          },
          "type": {
            "enum": [
              "web",
              "admin_ui",
              "spreadsheet",
              "dashboard",
              "form",
              "bot",
              "channel",
              "approval_center",
              "report",
              "api_portal",
              "cli"
            ]
          },
          "audience": {
            "enum": [
              "human",
              "agent",
              "both"
            ]
          },
          "mode": {
            "enum": [
              "ui",
              "agent_os",
              "api_mcp",
              "hybrid"
            ]
          },
          "software": {
            "type": "string",
            "description": "→ software[].id"
          },
          "instances": {
            "$ref": "#/$defs/refList",
            "description": "→ software[].instances[].id — an interface may be deployed to SEVERAL instances (prod and dev)"
          },
          "path": {
            "type": "string",
            "description": "RELATIVE path of the interface ('/admin/approvals'). Full URL = instance url + path — hence an interface has no absolute url of its own"
          },
          "related": {
            "$ref": "#/$defs/refList",
            "description": "→ interfaces[].id — related interfaces (flow: form → approval center → report)"
          },
          "roles": {
            "$ref": "#/$defs/refList",
            "description": "→ roles[].id — who can access it"
          },
          "entities": {
            "$ref": "#/$defs/refList"
          },
          "workflows": {
            "$ref": "#/$defs/refList"
          },
          "views": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "kanban | calendar | table | gallery…"
          },
          "languages": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "status": {
            "$ref": "#/$defs/status"
          }
        }
      }
    },
    "connections": {
      "type": "object",
      "description": "LENSES 5-6: MCP/API/CLI wiring — how an agent physically reaches each component",
      "properties": {
        "mcp": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "id"
            ],
            "properties": {
              "id": {
                "$ref": "#/$defs/slug"
              },
              "software": {
                "type": "string",
                "description": "→ software[].id"
              },
              "instance": {
                "type": "string",
                "description": "→ software[].instances[].id — which instance this MCP serves"
              },
              "transport": {
                "enum": [
                  "http",
                  "stdio",
                  "sse"
                ]
              },
              "auth": {
                "enum": [
                  "oauth",
                  "token_env",
                  "none"
                ]
              },
              "url_env": {
                "type": "string",
                "description": "NAME of the env key holding the URL"
              },
              "aggregator": {
                "type": "string",
                "description": "mcpware endpoint, if aggregated"
              },
              "capabilities": {
                "type": "string",
                "description": "What the agent can do through this MCP"
              }
            }
          }
        },
        "apis": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "software": {
                "type": "string"
              },
              "kind": {
                "type": "string",
                "description": "rest | graphql | s3 | sql"
              },
              "url_env": {
                "type": "string"
              }
            }
          }
        },
        "clis": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "software": {
                "type": "string"
              },
              "bin": {
                "type": "string"
              }
            }
          }
        }
      }
    },
    "agents": {
      "type": "object",
      "description": "Agent systematization — two types. STACK AGENTS: orchestrate the whole stack — read the entire stack including .claude/ (load skills, rules, commands) and MAY MODIFY the stack; runtime = an AI CLI (Claude Code, Codex, OpenClaw, Hermes); in the methodology this is the Super Agent / AI Client. MANAGED AGENTS: agents the stack manages as resources — invoked via API or used from an interface; defined by the triple: model (provider/model), instructions, and the list of MCP/API accesses (tools)",
      "properties": {
        "stack_agents": {
          "type": "array",
          "description": "Stack-level agents (outside the stack, with access to it). One agent may serve many stacks — it is declared in every macstack.json it has access to, with its own access list. hierarchy_role — position in the MACS hierarchy: orchestrator (level A: OpenClaw/Hermes — messenger frontend, receives the command, delegates, does NOT do the work), worker (level B: Claude Code/Codex in the stack directory — all the heavy work, reads .claude/, modifies the stack), control_plane (level C: governance, never runs LLM work). Delegation always goes downward",
          "items": {
            "type": "object",
            "required": [
              "id",
              "runtime"
            ],
            "properties": {
              "id": {
                "$ref": "#/$defs/slug",
                "description": "Agent identifier (openclaw-meg, claude-code-meg)"
              },
              "name": {
                "type": "string"
              },
              "runtime": {
                "enum": [
                  "claude-code",
                  "codex",
                  "openclaw",
                  "hermes",
                  "perplexity-computer",
                  "cursor",
                  "windsurf",
                  "gemini-cli",
                  "other"
                ],
                "description": "The AI CLI / runtime that brings the agent to life (\"the project is static, the CLI is the runtime\")"
              },
              "hierarchy_role": {
                "enum": [
                  "orchestrator",
                  "worker",
                  "control_plane"
                ]
              },
              "instance": {
                "type": "string",
                "description": "Deployment/instance name ({project}-{client}); where it lives — server, stack directory"
              },
              "tier": {
                "enum": [
                  1,
                  2
                ],
                "description": "Tier 1 = Plugins+Skills+MCP; Tier 2 = Skills+MCP"
              },
              "reads_stack": {
                "type": "boolean",
                "description": "Reads the whole stack including .claude/ (skills, rules, commands). true for a worker in the stack directory"
              },
              "can_modify_stack": {
                "type": "boolean",
                "description": "Permission to modify the stack (code, schemas, configs)"
              },
              "channels": {
                "type": "array",
                "items": {
                  "enum": [
                    "telegram",
                    "slack",
                    "discord",
                    "whatsapp",
                    "imessage",
                    "web",
                    "cli"
                  ]
                },
                "description": "Human communication channels (for the orchestrator)"
              },
              "modes": {
                "type": "array",
                "items": {
                  "enum": [
                    "manual",
                    "remote_interactive",
                    "remote_autonomous",
                    "headless"
                  ]
                }
              },
              "access": {
                "$ref": "#/$defs/refList",
                "description": "What the agent can access in THIS stack: → connections.mcp[].id | software[].id | interfaces[].id (cross-stack: '<stack-id>:<element-id>')"
              },
              "context_packs": {
                "$ref": "#/$defs/refList",
                "description": "→ context.packs[].name"
              },
              "delegates_to": {
                "$ref": "#/$defs/refList",
                "description": "→ stack_agents[].id — who it delegates to (always downward)"
              },
              "model": {
                "type": "string",
                "description": "provider/model (orchestrator — cheap, worker — strong)"
              },
              "billing": {
                "enum": [
                  "subscription_oauth",
                  "api_key"
                ]
              },
              "state": {
                "type": "string",
                "description": "Session persistence (--resume / state.json / manager)"
              },
              "invocations": {
                "type": "array",
                "description": "How the agent is started: channel (Telegram), manually, by a trigger (routine/cron), from a workflow (subprocess)",
                "items": {
                  "$ref": "#/$defs/agentInvocation"
                }
              }
            }
          }
        },
        "managed_agents": {
          "type": "array",
          "description": "Managed agents = model + instructions + tools (MCP/API accesses). Invoked via API, embedded in an interface (an AI Lawyer inside NocoBase), or run as a workflow step",
          "items": {
            "type": "object",
            "required": [
              "id",
              "name",
              "model"
            ],
            "properties": {
              "id": {
                "$ref": "#/$defs/slug"
              },
              "name": {
                "type": "string"
              },
              "role": {
                "type": "string",
                "description": "What it does (contract analysis, lead scoring…)"
              },
              "model": {
                "type": "string",
                "description": "provider/model: anthropic/claude-sonnet-4.6, openai/chatgpt-5.5"
              },
              "instructions": {
                "type": "string",
                "description": "System instructions (short; long ones go to instructions_ref)"
              },
              "instructions_ref": {
                "type": "string",
                "description": "Path/link to the full instructions file (do not duplicate inside macstack.json)"
              },
              "tools": {
                "type": "object",
                "description": "What the agent can access as tools",
                "properties": {
                  "mcp": {
                    "$ref": "#/$defs/refList",
                    "description": "→ connections.mcp[].id"
                  },
                  "apis": {
                    "$ref": "#/$defs/refList",
                    "description": "→ connections.apis (software id) | external APIs"
                  },
                  "workflows": {
                    "$ref": "#/$defs/refList",
                    "description": "→ workflows[].id — which workflows it may run"
                  },
                  "native": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "description": "Platform-native tools (knowledge-base-retrieve, dataQuery…)"
                  }
                }
              },
              "invocations": {
                "type": "array",
                "description": "How the agent is STARTED (may be several ways): via an interface (a user chats with it), from a workflow, by a trigger, by a direct API call",
                "items": {
                  "$ref": "#/$defs/agentInvocation"
                }
              },
              "knowledge_base": {
                "type": "string"
              },
              "languages": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "available_to": {
                "$ref": "#/$defs/refList",
                "description": "→ roles[].id"
              }
            }
          }
        }
      }
    },
    "context": {
      "type": "object",
      "description": "LENS 6: Claude plugins and Context Packs. technology = {tool}-{dev|ops|provision} (operations), stack = architecture descriptors, process = business workflows",
      "properties": {
        "plugins": {
          "type": "object",
          "description": "Plugins wired into this stack, grouped by plugin type. Each entry declares not only WHICH plugin, but WHAT IT COVERS in this stack (covers[]) — which sections of the stack file it is the reference for. That turns the plugin list from decoration into a routing table: given a task (add a workflow, add an entity), an agent knows which plugin to open instead of loading all of them or guessing.",
          "properties": {
            "technology": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/pluginRef"
              }
            },
            "process": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/pluginRef"
              }
            },
            "stack": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/pluginRef"
              }
            }
          }
        },
        "packs": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "name",
              "type"
            ],
            "properties": {
              "name": {
                "type": "string"
              },
              "type": {
                "enum": [
                  "development",
                  "operations",
                  "provisioning"
                ]
              },
              "plugins": {
                "type": "array",
                "items": {
                  "$ref": "#/$defs/slug"
                }
              }
            }
          }
        },
        "platforms": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Target AI clients (claude-code, codex, openclaw…)"
        },
        "marketplaces": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "docs": {
      "type": "object",
      "description": "The project's specification working set — the macstack/ folder. POINTERS ONLY: the markdown files own their text; this section owns where they live, which language their prose is written in, and which ID spaces they allocate. Section anchors (<!-- macstack:section=... -->) and ID tokens are NEVER translated; only headings and prose follow docs.language. docs/ stays the engineering folder — architecture, API conventions and runbooks do not move here.",
      "properties": {
        "root": {
          "type": "string",
          "default": "macstack",
          "description": "Folder holding macstack.json and the working documents. The spec resolves as <root>/macstack.json first, then a legacy ./macstack.json; both present is an error."
        },
        "language": {
          "type": "string",
          "description": "ISO 639-1 / BCP-47 code the documents' PROSE is written in (ru, en, de, uk). Absent = en. Deliberately NOT identity.language: a product can be German while its documents are Russian, and conflating the two hides a real decision."
        },
        "files": {
          "type": "object",
          "properties": {
            "overview": {
              "$ref": "#/$defs/docRef",
              "description": "What the product is, its goals, who it is for and the high-level processes - in plain words, plus the glossary that fixes the English terminology. Authored. Names the roles and points at automation, which owns their definition."
            },
            "user_cases": {
              "$ref": "#/$defs/docRef",
              "description": "Cases per role. Each carries a priority, its own UX requirements and an acceptance list whose bullets are individually addressable - the acceptance bar."
            },
            "ux_ui": {
              "$ref": "#/$defs/docRef",
              "description": "The interface bar: navigation, empty/loading/error states, responsive behaviour, accessibility and tone once for the whole product, then per screen what is on it, what can be done and what must NOT be visible there. Authored. Feeds interfaces[] id, name, path, roles."
            },
            "automation": {
              "$ref": "#/$defs/docRef",
              "description": "The trigger -> task -> workflow -> role model: who does what, what starts it, and what runs. Authored, read and corrected by the client. Writes the business half of this file: roles, processes[].tasks[], triggers[] and which workflow answers which trigger."
            },
            "handbook": {
              "$ref": "#/$defs/docRef",
              "description": "How to work with the platform, written for the people who will use it. Seeded from the cases and screens, then authored."
            },
            "open_questions": {
              "$ref": "#/$defs/docRef",
              "description": "SS A owed by the client - SS B deferred engineering, each with the trigger that ends the deferral."
            },
            "test_cases": {
              "$ref": "#/$defs/docRef",
              "description": "How each acceptance bullet is verified - one test per bullet, tagged auto or manual. Derived from user_cases, automation and ux_ui; its version names the versions it was derived from."
            },
            "architecture": {
              "$ref": "#/$defs/docRef",
              "description": "How the project is built, for the agent building in it - generated from software[], entities[], workflows[], connections and context.plugins."
            },
            "index": {
              "$ref": "#/$defs/docRef",
              "description": "Generated index of every case, screen and trigger plus the coverage count. The one index allowed to exist, because nobody maintains it by hand."
            },
            "readme": {
              "$ref": "#/$defs/docRef",
              "description": "The folder's own map, ownership table, ID spaces and loop - generated from the document contract, so the folder cannot describe itself differently from the way it is checked."
            },
            "tasks": {
              "$ref": "#/$defs/docRef",
              "description": "Milestones and tasks - the forward-looking half of the folder. Every task also lives in the team's own tracker, where the discussion happens; this file is the source of truth for what the work IS."
            },
            "decisions": {
              "$ref": "#/$defs/docRef",
              "description": "D-id registry grouped by the rulings file that holds the argument."
            },
            "changelog": {
              "$ref": "#/$defs/docRef",
              "description": "What reached the people who use the product, newest first. Curated from the work and release entries of the log - never a commit history in disguise."
            },
            "log": {
              "$ref": "#/$defs/docRef",
              "description": "Append-only journal of intakes, handoffs, merges, work and releases; the unprocessed signal and the development log are both read from it."
            }
          }
        },
        "dirs": {
          "type": "object",
          "description": "The four folders of the macstack/ layout, plus README.md and macstack.json at the root — six entries in all. The count is deliberate: a specification spread over many files becomes more tedious to review than the code it describes, which is the field's own criticism of spec-driven tooling. A fifth folder needs an argument that beats that.",
          "properties": {
            "client": {
              "type": "string",
              "default": "client",
              "description": "What a human writes and a client reads — the source of truth for what the product must do. This is the folder you hand over."
            },
            "generated": {
              "type": "string",
              "default": "generated",
              "description": "Built from a source, never edited by hand. A hand edit is lost on the next render and lint reports the difference."
            },
            "inbox": {
              "type": "string",
              "default": "inbox",
              "description": "What the client sent, exactly as sent. Immutable."
            },
            "history": {
              "type": "string",
              "default": "history",
              "description": "Journals and records: what happened, what was decided, what was checked, what we sent — plus the plan of work, which is the same working record read forwards."
            }
          }
        }
      }
    },
    "roles": {
      "type": "array",
      "description": "Roles and access (\"sees / can\" + isolation)",
      "items": {
        "type": "object",
        "required": [
          "id",
          "name"
        ],
        "properties": {
          "id": {
            "$ref": "#/$defs/slug"
          },
          "name": {
            "type": "string"
          },
          "sees": {
            "type": "string",
            "description": "One sentence: the role's thesis, not its case list. The cases live in USER-CASES.md. Lint warns past a sentence."
          },
          "can": {
            "type": "string",
            "description": "One sentence: what the role may do, in outline. Detail belongs to acl[] and to USER-CASES.md. Lint warns past a sentence."
          },
          "cases": {
            "type": "array",
            "description": "Case-ID prefixes or explicit ids this role owns in USER-CASES.md, e.g. ['C-*']. The join between the machine half of a role (id, acl, isolation) and its narrative half. Lint checks it in both directions.",
            "items": {
              "type": "string"
            }
          },
          "acl": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "entity": {
                  "type": "string",
                  "description": "→ entities[].id"
                },
                "fields": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "actions": {
                  "type": "array",
                  "items": {
                    "enum": [
                      "create",
                      "read",
                      "update",
                      "delete",
                      "approve",
                      "download"
                    ]
                  },
                  "description": "Generic verbs only. A domain-specific right is not a new verb: it is one of these plus a `condition`, plus `fields` when it touches only some of them. `approve_if_is_approver` was `approve` + a condition; `announce_payment_by_role` was `update` restricted to two fields the code already isolates."
                },
                "condition": {
                  "type": "string",
                  "maxLength": 200,
                  "description": "The row-level restriction on these actions, in plain words: \"only the confirmation this contact is the named approver for\". Prose on purpose — a predicate language here would be a second, weaker copy of the access rules the code already enforces, and the two would drift. This says WHAT is restricted so a reader knows to go and check; the code says how."
                }
              }
            }
          },
          "isolation": {
            "type": "string",
            "description": "Tenant isolation (e.g.: by the company field)"
          }
        }
      }
    },
    "cases": {
      "type": "array",
      "description": "What a person must be able to get done, per role — the skeleton of client/USER-CASES.md. The PROSE stays in that document: this holds the ids, the links and the state, so that every check can ask \"does the code do this\" and \"has the client confirmed it\" without parsing a paragraph. acceptance[] holds the ids of the acceptance bullets, never their text — the bullet is the client's sentence and the id is its address.",
      "items": {
        "type": "object",
        "required": [
          "id",
          "name"
        ],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^C?[A-Z]-[0-9]{2}$"
          },
          "name": {
            "type": "string"
          },
          "role": {
            "$ref": "#/$defs/slug",
            "description": "roles[].id — absent for a cross-cutting case"
          },
          "priority": {
            "enum": [
              "critical",
              "important",
              "nice-to-have"
            ]
          },
          "screens": {
            "description": "interfaces[].id",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "triggers": {
            "description": "triggers[].id",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "workflow": {
            "description": "workflows[].id",
            "type": "string"
          },
          "entities": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "acceptance": {
            "description": "ids of the acceptance bullets, e.g. C-04.a3 — positional within the case, so inserting one above another moves every id below it",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "status": {
            "$ref": "#/$defs/status"
          }
        }
      }
    },
    "prohibitions": {
      "type": "array",
      "description": "What the platform must REFUSE to do. A prohibition is not a case with a negative name: it has no acceptance list, because the refusal itself is the behaviour, and it belongs to no single role.",
      "items": {
        "type": "object",
        "required": [
          "id",
          "name"
        ],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^Z-[0-9]{2}$"
          },
          "name": {
            "type": "string"
          },
          "enforced_by": {
            "description": "workflows[].id or interfaces[].id",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "cases": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "status": {
            "$ref": "#/$defs/status"
          }
        }
      }
    },
    "invariants": {
      "type": "array",
      "description": "Rules that hold everywhere and are never negotiated away by a feature. They are stated once here and explained in client/OVERVIEW.md; a change to one is a decision, not an edit.",
      "items": {
        "type": "object",
        "required": [
          "id",
          "name"
        ],
        "properties": {
          "id": {
            "$ref": "#/$defs/slug"
          },
          "name": {
            "type": "string"
          },
          "enforced_by": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "status": {
            "$ref": "#/$defs/status"
          }
        }
      }
    },
    "glossary": {
      "type": "array",
      "description": "Terms the project uses in one exact sense. A domain word that is already Latin stays as it is — translating a term out of a contract makes it less precise, not more.",
      "items": {
        "type": "object",
        "required": [
          "id",
          "term"
        ],
        "properties": {
          "id": {
            "$ref": "#/$defs/slug"
          },
          "term": {
            "type": "string"
          },
          "scope": {
            "enum": [
              "domain",
              "platform",
              "legal",
              "technical"
            ]
          },
          "see": {
            "description": "entities[].id or workflows[].id this term names",
            "type": "string"
          }
        }
      }
    },
    "resources": {
      "type": "object",
      "description": "Input resources: accesses (env names + Infisical), external services, data, orchestration bindings",
      "properties": {
        "accesses": {
          "type": "array",
          "description": "Registry of the stack's env tokens — the codes of the required keys for .env. The macstack-dev scaffolder generates .env.example from this list; the linter checks that required keys exist in .env",
          "items": {
            "type": "object",
            "required": [
              "env"
            ],
            "properties": {
              "env": {
                "type": "string",
                "description": "The key NAME in .env (never the value)"
              },
              "required": {
                "type": "boolean",
                "default": true,
                "description": "Whether the token is required for the stack to work (false = only needed for an optional feature)"
              },
              "source": {
                "type": "string",
                "default": "infisical"
              },
              "project": {
                "type": "string"
              },
              "environment": {
                "type": "string"
              },
              "for": {
                "type": "string",
                "description": "→ software[].id | connections.mcp[].id"
              },
              "provided_by": {
                "enum": [
                  "client",
                  "us"
                ]
              }
            }
          }
        },
        "external_services": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "data_assets": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "bindings": {
          "type": "object",
          "description": "Service bindings, keyed by service (secrets manager, PaaS, git host, task tracker…). The task-tracker binding is what lets tooling reconcile TASKS.md with wherever the team actually discusses work — whichever tracker that is.",
          "additionalProperties": {
            "type": "object"
          }
        }
      }
    },
    "integrations": {
      "type": "array",
      "description": "External systems: status + approach + open questions",
      "items": {
        "type": "object",
        "required": [
          "id",
          "system"
        ],
        "properties": {
          "id": {
            "$ref": "#/$defs/slug"
          },
          "system": {
            "type": "string"
          },
          "direction": {
            "enum": [
              "pull",
              "push",
              "bidirectional",
              "tbd"
            ]
          },
          "approach": {
            "enum": [
              "api",
              "webhook",
              "mcp",
              "manual",
              "tbd"
            ]
          },
          "entities": {
            "$ref": "#/$defs/refList"
          },
          "status": {
            "$ref": "#/$defs/status"
          },
          "open_questions": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      }
    },
    "lifecycle": {
      "type": "object",
      "description": "Status along the 7 delivery stages of the Assembly Process",
      "properties": {
        "stage": {
          "enum": [
            "define",
            "decompose",
            "specify",
            "compose",
            "test",
            "deploy",
            "iterate"
          ]
        },
        "coverage_target": {
          "type": "string",
          "description": "The 70% → 95% principle"
        },
        "open_questions": {
          "type": "array",
          "description": "Open questions as POINTERS into OPEN-QUESTIONS.md. Prose belongs in the markdown, not here — a question long enough to need a paragraph is a question the JSON cannot keep in sync.",
          "items": {
            "oneOf": [
              {
                "type": "string",
                "description": "LEGACY free-text form. Still valid; lint warns and offers conversion to the pointer form."
              },
              {
                "$ref": "#/$defs/openItemRef"
              }
            ]
          }
        },
        "needs_from_client": {
          "type": "array",
          "description": "DERIVED VIEW of the live client-owed rows of OPEN-QUESTIONS.md §A — pointers only. Lint errors if it names a closed item or omits an open client input, so the two cannot disagree.",
          "items": {
            "oneOf": [
              {
                "type": "string",
                "description": "LEGACY free-text form. Still valid; lint warns and offers conversion to the pointer form."
              },
              {
                "allOf": [
                  {
                    "$ref": "#/$defs/openItemRef"
                  },
                  {
                    "properties": {
                      "id": {
                        "pattern": "^A[0-9]+$"
                      }
                    }
                  }
                ]
              }
            ]
          }
        },
        "decisions": {
          "type": "array",
          "description": "Owner and session rulings, as pointers into macstack/decisions/. Prose elsewhere in this file may cite a decision by id only if it resolves here.",
          "items": {
            "$ref": "#/$defs/decisionRef"
          }
        },
        "next_steps": {
          "type": "array",
          "description": "What happens next, as POINTERS into TASKS.md. Prose here rots the moment the plan moves; the task ids do not. Legacy free-text entries stay valid and lint offers the conversion.",
          "items": {
            "oneOf": [
              {
                "type": "string",
                "description": "LEGACY free-text form."
              },
              {
                "$ref": "#/$defs/taskRef"
              }
            ]
          }
        },
        "milestones": {
          "type": "array",
          "description": "The milestone list, as pointers into TASKS.md. A milestone is a coherent increment someone could review or demo; its falsifiable done_when checks live in the markdown.",
          "items": {
            "$ref": "#/$defs/milestoneRef"
          }
        },
        "updated": {
          "type": "string",
          "description": "Date of the last revision (YYYY-MM-DD)"
        }
      }
    },
    "commercial": {
      "type": "object",
      "description": "Commercial layer: the offer and the cost of ownership",
      "properties": {
        "offer": {
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "description": "Offer type from the Offer Catalog"
            },
            "price_range": {
              "type": "string"
            },
            "model": {
              "enum": [
                "one_time",
                "subscription",
                "retainer",
                "usage"
              ]
            }
          }
        },
        "cost_of_ownership": {
          "type": "object",
          "properties": {
            "items": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "name": {
                    "type": "string"
                  },
                  "usd_month": {
                    "type": [
                      "number",
                      "string"
                    ]
                  }
                }
              }
            },
            "total_usd_month": {
              "type": [
                "number",
                "string"
              ]
            }
          }
        }
      }
    }
  },
  "$defs": {
    "slug": {
      "type": "string",
      "pattern": "^[a-z0-9]+([-.][a-z0-9]+)*$"
    },
    "coverageArea": {
      "$ref": "#/$defs/slug",
      "description": "An area a plugin covers, from registry/coverage-areas.json. Two kinds: a SECTION of this stack file (results, processes, triggers, workflows, software, entities, interfaces, connections, agents, roles, resources, integrations) — meaning the plugin teaches you how to build that part of the stack; or a CROSS-CUTTING area not tied to one section (deployment, secrets-env, testing, observability, data-migration, security-access, conventions, documentation, cost-billing). Extension: a new area = a kebab-case slug + a PR to the registry."
    },
    "pluginRef": {
      "description": "A plugin wired into the stack. The object form is canonical: it says what the plugin covers here. A bare slug is the legacy short form, equivalent to {\"id\": slug} with no coverage declared — valid, but the linter warns, because an agent cannot route to a plugin whose coverage is unknown.",
      "anyOf": [
        {
          "$ref": "#/$defs/slug"
        },
        {
          "type": "object",
          "required": [
            "id"
          ],
          "properties": {
            "id": {
              "$ref": "#/$defs/slug",
              "description": "Plugin slug as published in the marketplace"
            },
            "covers": {
              "type": "array",
              "description": "What this plugin is the reference for IN THIS STACK. Example: directus-dev covers [software, entities, interfaces]; trigger-dev covers [workflows, triggers]; a stack plugin usually covers most sections at once. Values from registry/coverage-areas.json. The linter cross-checks this against the stack itself: a non-empty section that no plugin covers is a gap.",
              "items": {
                "$ref": "#/$defs/coverageArea"
              }
            },
            "scope": {
              "type": "array",
              "description": "Optional narrowing to the concrete elements this plugin owns — ids from software[], entities[], workflows[], interfaces[]. Needed when several plugins cover the same area: directus-dev covers `interfaces` with scope [directus], nextjs-dev covers `interfaces` with scope [nextjs]. Without scope an overlap is ambiguous and the linter warns.",
              "items": {
                "$ref": "#/$defs/slug"
              }
            },
            "note": {
              "type": "string",
              "description": "Free-form clarification for a human reader"
            }
          }
        }
      ]
    },
    "agenticChannel": {
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "const": "partial"
        }
      ]
    },
    "stackRef": {
      "type": "string",
      "description": "Reference to another macstack.json: 'github:owner/repo[#ref][/path]' | https URL to a repo or file | LOCAL folder absolute path ('/Users/me/STACKS/crm') | absolute path to a file. Folder/repo → <root>/macstack.json is used"
    },
    "agentInvocation": {
      "type": "object",
      "required": [
        "via"
      ],
      "description": "One way of starting an agent",
      "properties": {
        "via": {
          "enum": [
            "interface",
            "workflow",
            "trigger",
            "api",
            "channel",
            "manual"
          ],
          "description": "interface = a user talks to the agent through an interface (chat); workflow = invoked inside a workflow; trigger = started by a trigger; api = direct API call; channel = messenger channel (stack agents); manual = started by a human"
        },
        "interface": {
          "type": "string",
          "description": "→ interfaces[].id (for via=interface)"
        },
        "workflow": {
          "type": "string",
          "description": "→ workflows[].id (for via=workflow)"
        },
        "trigger": {
          "type": "string",
          "description": "→ triggers[].id (for via=trigger)"
        },
        "channel": {
          "type": "string",
          "description": "telegram | slack | … (for via=channel)"
        }
      }
    },
    "refList": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "status": {
      "enum": [
        "planned",
        "in_progress",
        "implemented",
        "partial",
        "ready_to_connect",
        "out_of_scope"
      ]
    },
    "docRef": {
      "type": "object",
      "description": "A pointer to one markdown document of the macstack/ folder. The document owns its text; this holds only where it is, who it is for, and which version the spec was reconciled against.",
      "required": [
        "path"
      ],
      "properties": {
        "path": {
          "type": "string",
          "description": "Path relative to docs.root"
        },
        "version": {
          "type": "string",
          "description": "CHECKED COPY: must equal the version in the document's own header and the last row of its journal table. Mismatch is a lint ERROR."
        },
        "updated": {
          "type": "string",
          "description": "Date of the last revision (YYYY-MM-DD)"
        },
        "audience": {
          "enum": [
            "client",
            "internal",
            "both"
          ],
          "description": "Who the document is written for. A client-facing document has its output language confirmed before writing."
        },
        "language": {
          "type": "string",
          "description": "Per-document override of docs.language (ISO 639-1)"
        },
        "reviewed": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
          "description": "Date this document was last CHECKED AGAINST THE CODE — not the date its text was edited. The two are different facts and conflating them is how a document stays confidently wrong: you can rewrite a sentence in USER-CASES today without ever asking whether the platform still behaves that way. A reviews/<date>-*-conformance.md newer than this date counts as the check and moves it forward."
        },
        "freshness_days": {
          "type": "integer",
          "minimum": 1,
          "default": 30,
          "description": "How long this document's last check stays good. Lint warns past it and errors past twice it — the same shape the delta age budget already uses (14/30). Set it per document: a business-logic document may outlive a user-cases document, and pretending one number fits both produces either noise or false comfort."
        }
      }
    },
    "openItemRef": {
      "type": "object",
      "description": "POINTER to one item of OPEN-QUESTIONS.md — never prose. The markdown owns the text and allocates the id; this holds the machine-readable state. A = owed by the client, B = deferred engineering. Numbers are never reused after a strike-through.",
      "required": [
        "id"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^Q?[AB][0-9]+$"
        },
        "ref": {
          "type": "string",
          "description": "Anchor into the document, e.g. 'OPEN-QUESTIONS.md#a5'"
        },
        "status": {
          "enum": [
            "open",
            "answered",
            "closed",
            "deferred"
          ]
        },
        "summary": {
          "type": "string",
          "maxLength": 200,
          "description": "CHECKED COPY, optional: must equal the first sentence of the referenced markdown item after whitespace normalization. Mismatch is a lint ERROR — this is the one denormalization the standard allows, and it may not rot silently."
        },
        "blocks": {
          "$ref": "#/$defs/refList",
          "description": "Elements this item blocks, e.g. 'results.first-invoice'"
        },
        "closed_by": {
          "type": "string",
          "pattern": "^D[0-9]+$",
          "description": "The decision that closed it → lifecycle.decisions[].id"
        },
        "closed_on": {
          "type": "string",
          "description": "YYYY-MM-DD"
        }
      }
    },
    "decisionRef": {
      "type": "object",
      "description": "POINTER to one ruling in macstack/decisions/. Exists because prose across the spec cites decisions by id, and a citation that cannot be followed is the same class of defect as a rotted file:line pointer.",
      "required": [
        "id"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^D[0-9]+$"
        },
        "ref": {
          "type": "string",
          "description": "Path to the rulings file, relative to docs.root"
        },
        "date": {
          "type": "string",
          "description": "YYYY-MM-DD"
        },
        "decided_by": {
          "enum": [
            "owner",
            "session"
          ]
        },
        "title": {
          "type": "string",
          "maxLength": 120
        }
      }
    },
    "taskRef": {
      "type": "object",
      "description": "POINTER to one task in TASKS.md — never prose. The markdown owns the description, the acceptance and the tracker id; this holds only enough to cross-check.",
      "required": [
        "id"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^M[0-9]+-T[0-9]+$",
          "description": "Milestone-scoped and permanent, e.g. M11-T9"
        },
        "ref": {
          "type": "string",
          "description": "Anchor into TASKS.md"
        },
        "status": {
          "enum": [
            "todo",
            "doing",
            "blocked",
            "done",
            "dropped"
          ]
        },
        "blocked_by": {
          "$ref": "#/$defs/refList",
          "description": "Task ids or open-item ids. An A-id here is how a client question becomes visibly load-bearing."
        }
      }
    },
    "milestoneRef": {
      "type": "object",
      "description": "POINTER to one milestone in TASKS.md. The done_when checks live in the markdown; this holds the id and the state.",
      "required": [
        "id"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^M[0-9]+$"
        },
        "title": {
          "type": "string",
          "maxLength": 120
        },
        "status": {
          "enum": [
            "todo",
            "doing",
            "blocked",
            "done",
            "dropped"
          ]
        },
        "ref": {
          "type": "string"
        }
      }
    }
  }
}
