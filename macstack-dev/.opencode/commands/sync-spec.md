---
description: Reconcile macstack.json against the client's authored tables (roles, tasks, triggers, screens)
---

Use the `macstack-dev:sync-spec` skill on the project at $ARGUMENTS (default: current directory).
Run the dry report first and show it. Apply only the value changes, and for every addition or
removal name what a human must decide — an id. Never invent an id.