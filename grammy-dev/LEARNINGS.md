# grammy-dev — Learnings

Accumulated feedback and fixes for the grammy-dev plugin. Populated by `/plugin-creator:feedback` and `/plugin-creator:wrap-up`.

Format per entry:

```markdown
## [DATE] — [skill-name]: Brief description

**Problem:** What went wrong
**Fix:** What was changed
**Root cause:** Why the original was wrong
**Severity:** Critical / Major / Minor
```
